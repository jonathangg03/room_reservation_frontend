import css from 'styled-jsx/css'
import { outline } from '../../theme'

const styles = css`
  section {
    outline: ${outline};
    background-color: white;
    width: 150px;
    height: 150px;
  }
`

export default styles